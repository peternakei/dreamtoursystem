# Seren Blue Safaris backend

See [the project setup guide](../README.md) and [migration notes](../FRONTEND/docs/MIGRATION.md).

Feature modules live under `app/Project/Modules`. Authentication lives under `app/Project/Auth`. Run the application from the project root with `./start.sh`.
