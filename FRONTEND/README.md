# Seren Blue Safaris frontend

Vue 3 and TypeScript with the shared UI components and theme from tzrestaurantsystem. See [the project setup guide](../README.md).

Run `npm run dev`, `npm run typecheck`, or `npm run build` in this folder. Module pages live in `src/pages/modules`, with shared list/detail/form components in `src/components/workspace`.
